apiVersion: v1
kind: Secret
metadata:
  name: crm-common-secret
  labels:
    app.kubernetes.io/part-of: enterprise-ai-crm
type: Opaque
stringData:
  # NOTE: Placeholder values for local/dev only. In real environments these are
  # injected from a secrets manager (Sealed Secrets / External Secrets / Vault)
  # and this file is never committed with real credentials.
  POSTGRES_USER: "crm"
  POSTGRES_PASSWORD: "change-me"
  JWT_SECRET: "change-me-in-production"
  JWT_PUBLIC_KEY: "change-me-in-production"
